package com.accessrisk.enums;

/**
 * Risk severity levels aligned with standard IAG (Identity Access Governance) frameworks.
 * Ordered from lowest to highest for comparison purposes.
 */
public enum Severity {
    LOW,
    MEDIUM,
    HIGH,
    CRITICAL
}
